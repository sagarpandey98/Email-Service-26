spring.application.name=EmailService
server.port=8080

# Email Configuration
# Values come from environment variables in production
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:sagaradavian@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:kwxa ariu fmha nbjo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.org.example.emailservice=INFO